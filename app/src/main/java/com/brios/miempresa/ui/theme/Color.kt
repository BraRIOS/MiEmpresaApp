package com.brios.miempresa.ui.theme

import androidx.compose.ui.graphics.Color

// Shared colors
val Red = Color(0xFFEA192F)
val DarkBlue = Color(0xFF001F4F)
val LightBlue = Color(0xFF04C4D9)
val Orange = Color(0xFFF27D16)
val DarkOrange = Color(0xFFF2561D)

// Light Mode Colors
val LightPrimary = DarkOrange    // Azul oscuro
val LightOnPrimary = Color.White  // Blanco sobre primario
val LightPrimaryContainer = Color(0xFFEC997B)  // Celeste claro para contenedores
val LightOnPrimaryContainer = DarkBlue  // Texto oscuro en contenedores primarios
val LightSecondary = Orange  // Naranja oscuro como secundario
val LightOnSecondary = Color.White  // Blanco sobre secundario
val LightSecondaryContainer = Orange  // Naranja más claro
val LightOnSecondaryContainer = DarkOrange  // Naranja oscuro para texto
val LightTertiary = LightBlue  // Celeste
val LightOnTertiary = DarkBlue  // Texto oscuro sobre celeste
val LightTertiaryContainer = Orange  // Naranja para contenedores terciarios
val LightOnTertiaryContainer = DarkOrange  // Texto oscuro en contenedores terciarios
val LightBackground = Color(0xFFFFFFFF)  // Blanco
val LightOnBackground = DarkBlue  // Texto azul oscuro sobre fondo claro
val LightSurface = Color(0xFFFFFFFF)  // Blanco para superficies
val LightOnSurface = DarkBlue  // Azul oscuro sobre superficies blancas
val LightError = Red  // Rojo para errores
val LightOnError = Color.White  // Blanco sobre rojo de error
val LightErrorContainer = DarkOrange  // Contenedor de error
val LightOnErrorContainer = Color.White  // Blanco sobre contenedor de error

// Dark Mode Colors
val DarkPrimary = LightBlue  // Celeste claro
val DarkOnPrimary = DarkBlue  // Azul oscuro sobre celeste
val DarkPrimaryContainer = DarkBlue  // Azul oscuro para contenedores primarios
val DarkOnPrimaryContainer = LightBlue  // Texto celeste sobre contenedores oscuros
val DarkSecondary = Orange  // Naranja claro
val DarkOnSecondary = DarkOrange  // Texto naranja oscuro sobre naranja claro
val DarkSecondaryContainer = DarkOrange  // Naranja oscuro para contenedores secundarios
val DarkOnSecondaryContainer = Color.White  // Blanco sobre contenedores secundarios
val DarkTertiary = DarkOrange  // Naranja oscuro
val DarkOnTertiary = Color.White  // Blanco sobre terciario oscuro
val DarkTertiaryContainer = Orange  // Naranja claro para contenedores terciarios
val DarkOnTertiaryContainer = DarkOrange  // Texto oscuro en contenedores claros
val DarkBackground = DarkBlue  // Azul oscuro para fondo
val DarkOnBackground = Color.White  // Blanco sobre fondo oscuro
val DarkSurface = DarkBlue  // Azul oscuro para superficies
val DarkOnSurface = Color.White  // Blanco sobre superficies oscuras
val DarkSurfaceVariant = DarkOrange  // Variante oscura de superficie
val DarkOnSurfaceVariant = Color.White  // Blanco sobre superficies variantes
val DarkError = Red  // Rojo para errores
val DarkOnError = Color.White  // Blanco sobre error
val DarkErrorContainer = DarkOrange  // Contenedor de error
val DarkOnErrorContainer = Color.White  // Blanco sobre contenedor de error
